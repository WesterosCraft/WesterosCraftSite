import { IReferenceGrid } from '@/models/sections/reference-grid';
import { Box } from '@chakra-ui/react';

type Props = {
	data: IReferenceGrid;
};

const ReferenceGrid = (data: Props) => {
	return <Box>referrence rgrid</Box>;
};

export default ReferenceGrid;
