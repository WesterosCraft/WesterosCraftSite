#import "./sliceFragments.graphql"

{
  entries {
    ... on homePage_homePage_Entry {
      heading
      subheading
      backgroundImage {
        url
      }
      slices {
        ...video
        ...animatedImageWithText
        ...featuredBanner
        ...textBanner
        ...text
      }
    }
  }
}
