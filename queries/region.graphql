query($region: String) {
  entries(search: $region) {
    ... on wikiDestination_wikiDestination_Entry {
      slug
      title
      region
      locationType
      synopsis
      images {
        url
      }
    }
  }
}
