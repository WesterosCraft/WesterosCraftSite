query($destination: [String]) {
  entries(slug: $destination) {
    ... on wikiDestination_wikiDestination_Entry {
      title
      region
      redoAvailabe
      serverBuild
      locationStatus
      locationType
      dateStarted
      dateCompleted
    }
  }
}
