query($section: [String]) {
  entries(section: $section) {
    ... on wikiDestination_wikiDestination_Entry {
      title
      region
      redoAvailabe
      serverBuild
      locationStatus
      locationType
      dateStarted
      dateCompleted
    }
  }
}
