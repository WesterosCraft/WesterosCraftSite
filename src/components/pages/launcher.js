import React from 'react';
import { Text, Flex, Box } from 'rebass';
import Layout from '../components/templates/layout/layout';

const LauncherPage = () => {
  return (
    <Layout>
      <Flex>
        <Text> Launcher PAGE </Text>{' '}
      </Flex>{' '}
    </Layout>
  );
};

export default LauncherPage;
